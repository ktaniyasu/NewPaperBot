# =========================================================
# Copy this file to `.env` and fill secrets before running.
# NEVER commit real secrets. Use dummy values in examples.
# =========================================================

# --- LLM Provider selection ---
# gemini | openrouter | openai(将来互換)
LLM_PROVIDER=gemini

# (Gemini) モデルは未指定なら既定: models/gemini-2.5-flash-preview-05-20
# LLM_PROVIDER=gemini の場合のみ有効。models/ プレフィックス必須。
# 例) LLM_MODEL=models/gemini-1.5-flash-latest
# LLM_MODEL=

# (OpenRouter) 使用する場合は必須
# 例) openai/gpt-4o-mini, openai/gpt-4o
# LLM_PROVIDER=openrouter
# LLM_MODEL=openai/gpt-4o-mini
# OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# --- API Keys (Secrets) ---
# 必須。OpenRouter利用時でも型上必須なためダミー可（実際には未使用）
GOOGLE_API_KEY=dummy

# --- Discord Bot (任意。Bot連携する場合のみ) ---
# Bot トークン（なければヘッドレスで動作）
# DISCORD_BOT_TOKEN=
# サーバ（ギルド）ID（任意）
# DISCORD_GUILD_ID=

# Discord 送信先と arXiv カテゴリの対応（連番で複数可）
# 数値のみ（例: 123456789012345678）
# ARXIV_CATEGORY_1=cs.LG
# DISCORD_CHANNEL_ID_1=123456789012345678
# ARXIV_CATEGORY_2=cs.AI
# DISCORD_CHANNEL_ID_2=123456789012345679

# --- ArXiv API ---
ARXIV_MAX_RESULTS=100
ARXIV_RETRY_ATTEMPTS=3
ARXIV_TIMEOUT=30
ARXIV_QPS=1.0
ARXIV_CONCURRENCY=1

# --- PDF chunking ---
PDF_CHUNK_SIZE=1000
PDF_CHUNK_OVERLAP=200

# --- RAG settings ---
RAG_USE_FAISS=false
RAG_EMBED_MODEL=Qwen/Qwen3-Embedding-0.6B
RAG_TOP_K=6
RAG_MIN_SIMILARITY=0.2
RAG_EMBED_CACHE=true
RAG_EMBED_CACHE_DIR=cache/embeddings
RAG_INDEX_PERSIST=true
RAG_INDEX_DIR=cache/faiss

# --- Database ---
DATABASE_URL=sqlite:///data/arxiv_papers.db

# --- Logging ---
LOG_LEVEL=INFO
LOG_FILE=logs/arxiv_analyzer.log
LOG_JSON=false

# --- LLM runtime params ---
LLM_THINKING_BUDGET=1024
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8192
LLM_CONTEXT_WINDOW_TOKENS=200000
LLM_REQUEST_TIMEOUT=60
LLM_STRICT_JSON=true
LLM_USE_TOOLS=true
LLM_CONCURRENCY=3

# --- HTTP common (PDF download etc.) ---
HTTP_REQUEST_TIMEOUT=60
HTTP_RETRY_ATTEMPTS=3
HTTP_QPS=4.0
HTTP_CONCURRENCY=4

# --- Scheduler ---
# true にすると起動直後に 1 回走ります（既定は平日 8:30）
DEBUG_RUN_IMMEDIATELY=false